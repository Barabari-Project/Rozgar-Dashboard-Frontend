import { IModule } from "../../utils/types/course";

interface VideoTabProps {
    sectionTitle: string | null;
    module: IModule | null;
}

const VideoTab: React.FC<VideoTabProps> = ({ sectionTitle, module }) => {
    return (
        <></>
    )
}

export default VideoTab;