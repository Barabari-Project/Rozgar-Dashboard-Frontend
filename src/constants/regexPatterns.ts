export const nameRegex = /^[a-zA-Z]{2,50}$/;
export const emailRegex = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;
export const passwordRegex = /^.{4,9}$/;
export const pincodeRegex = /^\d{6}$/;
export const phoneRegex = /^\d{10}$/;
export const addressRegex = /^[a-zA-Z ]{2,50}$/;